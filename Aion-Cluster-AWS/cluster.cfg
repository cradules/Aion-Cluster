BROTHER="Hostname of the other vm on cluster"
BROTHERIP="IP of the other vm on cluster"
APPPORT="Application port"
AZGROUP="Azure resource group"
SHAREDDISK="Disk that is shared betwen instances"
DATE=$(date +"%T")
LOGFILE=/var/log/cluster.log
